import React from 'react';
import styed from 'styled-components';

const HeaderBlock = styed.div``;

const Header = () => {
  return (
    <HeaderBlock>

    </HeaderBlock>
  );
};

export default Header;
